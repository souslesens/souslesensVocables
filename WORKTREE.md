# Worktree — Issue #1796

**Branch:** `claude/fix-issue-1796`

| Service  | URL |
|----------|-----|
| Backend  | http://localhost:4206 |
| Frontend | http://localhost:6369 |

## Start everything
```bash
./start-worktree.sh        # Linux/Mac
start-worktree.cmd         # Windows
```
Or manually: `PORT=4206 npm run start`

> **Vite config:** using `mainapp/vite.config.worktree.js` (generated — do not commit)
