// Generated by github-issue-claude-code — do not commit
// Extends the base vite.config.js with worktree-specific port isolation.
import { mergeConfig } from 'vite';
import baseConfig from './vite.config.js';

export default mergeConfig(baseConfig, {
  server: {
    port: parseInt(process.env.VITE_PORT || '6369'),
    proxy: {
      '/api': `http://localhost:${process.env.VITE_BACKEND_PORT || '4206'}`,
    },
  },
});
