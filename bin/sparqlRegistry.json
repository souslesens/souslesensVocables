[
    {
        "name": "getSourceTaxonomyPredicates",
        "module": "Sparql_OWL",
        "description": "Builds the SPARQL property path expressing the parent/child (taxonomy) relation for a source. Uses `options.specificPredicates` if provided, otherwise the source's configured `taxonomyPredicates`, falling back to `rdfs:subClassOf`. Multiple predicates are joined with `|`; `options.memberPredicate` appends `^rdfs:member`.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name whose `taxonomyPredicates` are read (null → default `rdfs:subClassOf`)"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Predicate options",
                "properties": [
                    {
                        "name": "specificPredicates",
                        "type": "(string|string[])",
                        "required": false,
                        "description": "Predicate(s) to use instead of the source config"
                    },
                    {
                        "name": "memberPredicate",
                        "type": "boolean",
                        "required": false,
                        "description": "Append `^rdfs:member` to the path"
                    }
                ]
            }
        ],
        "returns": {
            "type": "string",
            "description": "A SPARQL property-path string defining the taxonomy relation"
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "getTopConcepts",
        "module": "Sparql_OWL",
        "description": "Returns the top-level classes of an OWL source: `owl:Class` instances with no named parent class in the queried graphs. Blank nodes are excluded; labels are fetched optionally and language-filtered. **Top-class filter priority** (first match wins): 1. `options.skipTopClassFilter` → no filter at all 2. `sources.json[source].topClassFilter` (non-empty, non-`\"_default\"`) → custom SPARQL filter string 3. Default {@link module:Sparql_OWL.defaultTopClassFilter}: no parent that is a named class of the queried graphs **Other source-level config knobs** (`sources.json`): - `schemaType: \"KNOWLEDGE_GRAPH\"` — omits the `?topConcept rdf:type owl:Class.` triple - `sparql_server.no_params` — sends query as plain URL body instead of `?query=` param",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name to query"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "selectGraph",
                        "type": "boolean",
                        "required": false,
                        "description": "Also bind each triple's named graph (`?subjectGraph`)"
                    },
                    {
                        "name": "withoutImports",
                        "type": "boolean",
                        "required": false,
                        "description": "Exclude imported graphs from the `FROM` clause"
                    },
                    {
                        "name": "skipTopClassFilter",
                        "type": "boolean",
                        "required": false,
                        "description": "Do not apply any top-class filter"
                    },
                    {
                        "name": "filter",
                        "type": "string",
                        "required": false,
                        "description": "Extra SPARQL filter appended after the top-class filter"
                    },
                    {
                        "name": "filterCollections",
                        "type": "(string|string[])",
                        "required": false,
                        "description": "Restrict to top concepts reachable from these SKOS collections"
                    },
                    {
                        "name": "returnQueryStr",
                        "type": "boolean",
                        "required": false,
                        "description": "Return the SPARQL query string instead of executing it"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `topConcept`, `topConceptLabel` (optional), `subjectGraph` (optional)."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null,
        "summary": "Returns the top-level classes of an OWL source: `owl:Class` instances with no named parent class in the queried graphs. Blank nodes are excluded; labels are fetched optionally and language-filtered."
    },
    {
        "name": "getNodeChildren",
        "module": "Sparql_OWL",
        "description": "Returns the children of node(s) down to a given depth in an OWL class hierarchy. Matches `?child1 <taxonomyPredicate> ?subject` for the parent (`ids` or `words`), then chains nested `OPTIONAL` blocks `?childN+1 <taxonomyPredicate> ?childN` with their labels. A `filterCollections` option switches to a SKOS-collection-scoped variant.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name to query"
            },
            {
                "name": "words",
                "type": "(string|string[])",
                "required": true,
                "description": "Label word(s) for fuzzy/exact parent match; mutually exclusive with `ids`"
            },
            {
                "name": "ids",
                "type": "(string|string[])",
                "required": true,
                "description": "Parent class URI(s) whose children are fetched"
            },
            {
                "name": "descendantsDepth",
                "type": "number",
                "required": true,
                "description": "Number of taxonomy levels to expand"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "selectGraph",
                        "type": "boolean",
                        "required": false,
                        "description": "Bind each triple's named graph"
                    },
                    {
                        "name": "exactMatch",
                        "type": "boolean",
                        "required": false,
                        "description": "Treat `words` as exact rather than regex matches"
                    },
                    {
                        "name": "withoutImports",
                        "type": "boolean",
                        "required": false,
                        "description": "Exclude imported graphs from the `FROM` clause"
                    },
                    {
                        "name": "specificPredicates",
                        "type": "(string|string[])",
                        "required": false,
                        "description": "Override taxonomy predicate(s) used for the parent/child relation"
                    },
                    {
                        "name": "filter",
                        "type": "string",
                        "required": false,
                        "description": "Extra SPARQL filter appended to the query"
                    },
                    {
                        "name": "filterCollections",
                        "type": "(string|string[])",
                        "required": false,
                        "description": "Restrict children to members of these SKOS collections"
                    },
                    {
                        "name": "sort",
                        "type": "string",
                        "required": false,
                        "description": "Variable name to order results by"
                    },
                    {
                        "name": "limit",
                        "type": "number",
                        "required": false,
                        "description": "Result limit (defaults to `Config.queryLimit`)"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `subject`, `subjectLabel` (optional), `child1`, `child1Label` (optional), `child1Type` (optional), `child1Graph` (optional), depth-indexed `childN` (optional), `childNLabel` (optional), `collection` (optional), `acollection` (optional), `collectionLabel` (optional), `acollectionLabel` (optional)."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "getNodeInfos",
        "module": "Sparql_OWL",
        "description": "Returns all triples describing a resource (`<conceptId> ?prop ?value`), optionally also the inverse triples where it is the object, with property/value labels. Can exclude blank nodes and OWL restrictions.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name to query"
            },
            {
                "name": "conceptId",
                "type": "string",
                "required": true,
                "description": "URI of the resource to describe"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "selectGraph",
                        "type": "boolean",
                        "required": false,
                        "description": "Bind each triple's named graph"
                    },
                    {
                        "name": "getValuesLabels",
                        "type": "boolean",
                        "required": false,
                        "description": "Add `rdfs:label` of properties and values"
                    },
                    {
                        "name": "inverseProperties",
                        "type": "boolean",
                        "required": false,
                        "description": "Also return triples where the resource is the object"
                    },
                    {
                        "name": "noRestrictions",
                        "type": "boolean",
                        "required": false,
                        "description": "Exclude values that are `owl:Restriction`"
                    },
                    {
                        "name": "limit",
                        "type": "number",
                        "required": false,
                        "description": "Result limit (defaults to `Config.queryLimit`)"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `prop`, `value`, `propLabel` (optional), `valueLabel` (optional), `g` (optional)."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "getNodeParents",
        "module": "Sparql_OWL",
        "description": "Returns the ancestors of node(s) up to a given depth in an OWL hierarchy. Builds nested `OPTIONAL` blocks chaining `?broaderN-1 rdfs:subClassOf|rdf:type ?broaderN` (excluding `owl:Restriction`/`owl:Class`), grouping each node's types/superclasses/graphs via `GROUP_CONCAT` with an explicit `GROUP BY` per (subject, broader, labels). Depth is capped at 9 (Virtuoso allows at most 20 GROUP BY / hash-join keys: 2 fixed + 2 per level, so 2 + 2*9 = 20).",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name to query"
            },
            {
                "name": "words",
                "type": "(string|string[])",
                "required": true,
                "description": "Label word(s) identifying the start node; mutually exclusive with `ids`"
            },
            {
                "name": "ids",
                "type": "(string|string[])",
                "required": true,
                "description": "Start class URI(s) whose ancestors are fetched"
            },
            {
                "name": "ancestorsDepth",
                "type": "number",
                "required": true,
                "description": "Number of levels to climb (capped as described above)"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "selectGraph",
                        "type": "boolean",
                        "required": false,
                        "description": "Bind broader nodes' named graphs"
                    },
                    {
                        "name": "withoutImports",
                        "type": "boolean",
                        "required": false,
                        "description": "Exclude imported graphs from the `FROM` clause"
                    },
                    {
                        "name": "excludeType",
                        "type": "boolean",
                        "required": false,
                        "description": "Reserved flag to drop type variables from the SELECT clause"
                    },
                    {
                        "name": "filterCollections",
                        "type": "(string|string[])",
                        "required": false,
                        "description": "Exclude ancestors that are members of these collections"
                    },
                    {
                        "name": "limit",
                        "type": "number",
                        "required": false,
                        "description": "Result limit (defaults to `Config.queryLimit`)"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `subject`, `subjectLabel` (optional), `subjectTypes`, `subjectSuperClasses` (optional), `broader1`, `broader1Label` (optional), `broaderGraphs1`, and depth-indexed `broaderN` (optional), `broaderNLabel` (optional), `broaderGraphsN`."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "getNodesAncestorsOrDescendants",
        "module": "Sparql_OWL",
        "description": "Returns the ancestor or descendant hierarchy of given class(es), one ordered branch list per class, `options.descendants` choosing the direction: how you specialise a class or enumerate its subtypes. Returns nothing when the children are attached through an OWL restriction rather than a direct `rdfs:subClassOf`, in which case walk the restriction blank nodes with {@link module:Sparql_OWL.getFilteredTriples}. Runs a nested `?class rdfs:subClassOf{0,5}[+] ?superClass` query (direction depends on `options.descendants`) and rebuilds, for each input class, its hierarchy as an array of hierarchy items in JS, skipping blank-node superclasses. Used by `OntologyModels` to fetch the hierarchies of a set of nodes (start/end nodes).",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name to query"
            },
            {
                "name": "classIds",
                "type": "(string|string[])",
                "required": true,
                "description": "Class URI(s) whose hierarchy is built"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "descendants",
                        "type": "boolean",
                        "required": false,
                        "description": "Build descendant hierarchies instead of ancestors"
                    },
                    {
                        "name": "excludeItself",
                        "type": "boolean",
                        "required": false,
                        "description": "Use `+` instead of `*` (exclude the class itself)"
                    },
                    {
                        "name": "filter",
                        "type": "string",
                        "required": false,
                        "description": "Extra SPARQL filter appended to the query"
                    },
                    {
                        "name": "withoutImports",
                        "type": "boolean",
                        "required": false,
                        "description": "Exclude imported graphs from the `FROM` clause"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Object",
            "description": "Throws an error or returns `{hierarchies}`; each hierarchy entry is a binding with `subject`, `class`, `type`, `classLabel` (optional), `superClass`, `superClassType`, `superClassSubClass`, `superClassLabel` (optional), `subjectTypes`."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "summary": "Returns the ancestor or descendant hierarchy of given class(es), one ordered branch list per class, `options.descendants` choosing the direction: how you specialise a class or enumerate its subtypes. Returns nothing when the children are attached through an OWL restriction rather than a direct `rdfs:subClassOf`, in which case walk the restriction blank nodes with {@link module:Sparql_OWL.getFilteredTriples}.",
        "access": "read",
        "mcpTool": "sls_node_descendants",
        "mcpFixed": {
            "options.descendants": true,
            "options.excludeItself": true
        }
    },
    {
        "name": "getFilteredTriples2",
        "module": "Sparql_OWL",
        "description": "Alternative to {@link module:Sparql_OWL.getFilteredTriples} that resolves the type and label of subject, property and object from any graph, then matches the actual triples in the source's own graph. Filters by subject/property/object ids. Not historical — selected over `getFilteredTriples` on demand: `Lineage_whiteboard` toggles between the two via `options.getFilteredTriples2`, and `nodeRelations_bot` calls it directly to extract a node's distinct properties (`options.distinct = \"?prop ?propLabel\"`). Deliberately not `@expose`d: {@link module:Sparql_OWL.getFilteredTriples} is the API-facing variant. It batches large id lists, excludes literal objects by default and requires no `options` argument, whereas this one dereferences `options` unguarded and matches `?prop ?x ?propType` across every graph.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name to query"
            },
            {
                "name": "subjectIds",
                "type": "(string|string[])",
                "required": false,
                "description": "Subject URI(s) to match (optional)"
            },
            {
                "name": "propertyIds",
                "type": "(string|string[])",
                "required": false,
                "description": "Property URI(s) to match (optional)"
            },
            {
                "name": "objectIds",
                "type": "(string|string[])",
                "required": false,
                "description": "Object URI(s) to match (optional)"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "withImports",
                        "type": "boolean",
                        "required": false,
                        "description": "Include imported graphs in the `FROM` clause (imports are excluded by default here)"
                    },
                    {
                        "name": "distinct",
                        "type": "string",
                        "required": false,
                        "description": "Variables for the SELECT clause (defaults to `*`)"
                    },
                    {
                        "name": "filter",
                        "type": "string",
                        "required": false,
                        "description": "Extra SPARQL filter appended to the query"
                    },
                    {
                        "name": "limit",
                        "type": "number",
                        "required": false,
                        "description": "Result limit (defaults to `Config.queryLimit`)"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `subject`, `subjectType`, `subjectLabel` (optional), `prop`, `x`, `propType`, `propLabel` (optional), `object`, `objectType`, `objectLabel` (optional), `g`."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null,
        "summary": "Alternative to {@link module:Sparql_OWL.getFilteredTriples} that resolves the type and label of subject, property and object from any graph, then matches the actual triples in the source's own graph. Filters by subject/property/object ids."
    },
    {
        "name": "getFilteredTriples",
        "module": "Sparql_OWL",
        "description": "Returns triples filtered by any combination of subject/property/object ids, with subject, property and object labels and types. The traversal workhorse: pass `objectIds` to find what points at a node, which is how OWL restriction blank nodes get resolved. Literal objects are excluded by default. Subject or object id lists are sliced into `Sparql_generic.slicesSize` batches and queried with `async.eachSeries`, concatenating results. Canonical, widely used triple-fetch helper (Lineage, MappingModeler, bots). See {@link module:Sparql_OWL.getFilteredTriples2} for the cross-graph variant.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name to query"
            },
            {
                "name": "subjectIds",
                "type": "(string|string[])",
                "required": false,
                "description": "Subject URI(s) to match (optional)"
            },
            {
                "name": "propertyIds",
                "type": "(string|string[])",
                "required": false,
                "description": "Property URI(s) to match (optional)"
            },
            {
                "name": "objectIds",
                "type": "(string|string[])",
                "required": false,
                "description": "Object URI(s) to match (optional)"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "filter",
                        "type": "string",
                        "required": false,
                        "description": "Extra SPARQL filter appended to the query"
                    },
                    {
                        "name": "onlyObjectProperties",
                        "type": "boolean",
                        "required": false,
                        "description": "Restrict to `owl:ObjectProperty` predicates"
                    },
                    {
                        "name": "onlyDataTypeProperties",
                        "type": "boolean",
                        "required": false,
                        "description": "Restrict to literal objects"
                    },
                    {
                        "name": "includeLiterals",
                        "type": "boolean",
                        "required": false,
                        "description": "Allow literal objects (otherwise excluded)"
                    },
                    {
                        "name": "withoutImports",
                        "type": "boolean",
                        "required": false,
                        "description": "Exclude imported graphs from the `FROM` clause"
                    },
                    {
                        "name": "distinct",
                        "type": "string",
                        "required": false,
                        "description": "Variables for the SELECT clause (defaults to `*`)"
                    },
                    {
                        "name": "limit",
                        "type": "number",
                        "required": false,
                        "description": "Result limit (defaults to `Config.queryLimit`)"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `subject`, `prop`, `object`, `subjectType` (optional), `subjectLabel` (optional), `propLabel` (optional), `objectType` (optional), `objectValue` (optional), `objectLabel` (optional)."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "summary": "Returns triples filtered by any combination of subject/property/object ids, with subject, property and object labels and types. The traversal workhorse: pass `objectIds` to find what points at a node, which is how OWL restriction blank nodes get resolved. Literal objects are excluded by default.",
        "access": "read",
        "mcpTool": "sls_filtered_triples"
    },
    {
        "name": "getObjectPropertiesDomainAndRange",
        "module": "Sparql_OWL",
        "description": "Returns object (or datatype) properties with their domains, ranges, sub-properties and inverse properties: what links what to what in a source, which is how you answer \"can these two classes be related, and through which property\". Omit `domainIds` for the whole source's property schema. Annotation properties and plain RDF/OWL vocabulary legitimately have no declared domain or range, so an empty answer for one of those is not a defect. The id filter can target the domain, range, prop or subProp depending on options, and a `searchType` mode searches by words on prop/domain/range. With `addInverseRestrictions`, recursively merges the inverse-direction results.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name to query"
            },
            {
                "name": "domainIds",
                "type": "(string|string[])",
                "required": false,
                "description": "Domain (or range) class URI(s) to filter by (optional)"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "inverseRestriction",
                        "type": "boolean",
                        "required": false,
                        "description": "Filter on range instead of domain"
                    },
                    {
                        "name": "propIds",
                        "type": "(string|string[])",
                        "required": false,
                        "description": "Filter by property URI(s)"
                    },
                    {
                        "name": "subPropIds",
                        "type": "(string|string[])",
                        "required": false,
                        "description": "Filter by sub-property URI(s)"
                    },
                    {
                        "name": "dataTypeProperties",
                        "type": "boolean",
                        "required": false,
                        "description": "Query `owl:DatatypeProperty` instead of `owl:ObjectProperty`"
                    },
                    {
                        "name": "searchType",
                        "type": "string",
                        "required": false,
                        "description": "One of `property`/`domain`/`range` to search by `options.words`"
                    },
                    {
                        "name": "words",
                        "type": "(string|string[])",
                        "required": false,
                        "description": "Label word(s) used when `searchType` is set"
                    },
                    {
                        "name": "addInverseRestrictions",
                        "type": "boolean",
                        "required": false,
                        "description": "Also fetch and merge inverse-direction results"
                    },
                    {
                        "name": "filter",
                        "type": "string",
                        "required": false,
                        "description": "Extra SPARQL filter (replaces the id filter)"
                    },
                    {
                        "name": "selectGraph",
                        "type": "boolean",
                        "required": false,
                        "description": "Bind each triple's named graph"
                    },
                    {
                        "name": "limit",
                        "type": "number",
                        "required": false,
                        "description": "Result limit (defaults to `Config.queryLimit`)"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `domain` (optional), `domainLabel` (optional), `prop`, `propLabel` (optional), `range` (optional), `rangeLabel` (optional), `subProp` (optional), `subPropLabel` (optional), `inverseProp` (optional), `inversePropLabel` (optional)."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "summary": "Returns object (or datatype) properties with their domains, ranges, sub-properties and inverse properties: what links what to what in a source, which is how you answer \"can these two classes be related, and through which property\". Omit `domainIds` for the whole source's property schema. Annotation properties and plain RDF/OWL vocabulary legitimately have no declared domain or range, so an empty answer for one of those is not a defect.",
        "access": "read"
    },
    {
        "name": "getObjectRestrictions",
        "module": "Sparql_OWL",
        "description": "Returns the property triples a class carries as OWL restrictions: property, constraint type, target value and cardinality, with their labels. This is how a class declares its attributes, so it is the answer to \"what can I say about an instance of this class\". Returns nothing when the class carries no direct restriction, in which case try its parent. Matches `?subject rdfs:subClassOf ?node`, `?node rdf:type owl:Restriction`, `?node owl:onProperty ?prop` and the constraint value (`owl:someValuesFrom`/`allValuesFrom`/`hasValue`/`onClass`) plus any cardinality. Property and value labels are filled afterwards (including from imported sources via {@link module:Sparql_OWL.getLabelsMap}).",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name to query"
            },
            {
                "name": "subClassIds",
                "type": "(string|string[])",
                "required": false,
                "description": "Class URI(s) carrying the restrictions (or restriction values when `inverseRestriction`)"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "inverseRestriction",
                        "type": "boolean",
                        "required": false,
                        "description": "Filter on the restriction value (range) instead of the subject"
                    },
                    {
                        "name": "restrictionIds",
                        "type": "(string|string[])",
                        "required": false,
                        "description": "Filter by restriction value URI(s)"
                    },
                    {
                        "name": "listPropertiesOnly",
                        "type": "boolean",
                        "required": false,
                        "description": "Select only `?prop`/`?propLabel`"
                    },
                    {
                        "name": "withoutBlankNodes",
                        "type": "boolean",
                        "required": false,
                        "description": "Select an explicit non-blank-node variable set"
                    },
                    {
                        "name": "turtle",
                        "type": "boolean",
                        "required": false,
                        "description": "Emit a CONSTRUCT query"
                    },
                    {
                        "name": "someValuesFrom",
                        "type": "boolean",
                        "required": false,
                        "description": "Restrict to `owl:someValuesFrom`/`owl:onClass`"
                    },
                    {
                        "name": "allValuesFrom",
                        "type": "boolean",
                        "required": false,
                        "description": "Restrict to `owl:allValuesFrom`/`owl:onClass`"
                    },
                    {
                        "name": "hasValue",
                        "type": "boolean",
                        "required": false,
                        "description": "Restrict to `owl:hasValue`/`owl:onClass`"
                    },
                    {
                        "name": "getMetadata",
                        "type": "boolean",
                        "required": false,
                        "description": "Also fetch status/creator/date/provenance metadata of each restriction"
                    },
                    {
                        "name": "withoutImports",
                        "type": "boolean",
                        "required": false,
                        "description": "Exclude imported graphs from the `FROM` clause"
                    },
                    {
                        "name": "filter",
                        "type": "string",
                        "required": false,
                        "description": "Extra SPARQL filter (its `object` is mapped to `value`)"
                    },
                    {
                        "name": "limit",
                        "type": "number",
                        "required": false,
                        "description": "Result limit (defaults to `Config.queryLimit`)"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `subject`, `subjectLabel` (optional), `node`, `prop`, `propLabel` (optional), `constraintType`, `value`, `valueLabel` (optional), `cardinalityType` (optional), `cardinalityValue` (optional), `g` (optional), `status` (optional), `creationDate` (optional), `creator` (optional), `provenance` (optional), `domainSourceLabel` (optional), `rangeSourceLabel` (optional)."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "summary": "Returns the property triples a class carries as OWL restrictions: property, constraint type, target value and cardinality, with their labels. This is how a class declares its attributes, so it is the answer to \"what can I say about an instance of this class\". Returns nothing when the class carries no direct restriction, in which case try its parent.",
        "access": "read",
        "mcpTool": "sls_node_properties"
    },
    {
        "name": "getNamedIndividuals",
        "module": "Sparql_OWL",
        "description": "Returns the `owl:NamedIndividual` instances of given class(es): matches `?subject rdf:type ?node` (filtered by `ids`) and `?subject rdf:type owl:NamedIndividual`, with subject and class labels.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name to query"
            },
            {
                "name": "ids",
                "type": "(string|string[])",
                "required": false,
                "description": "Class URI(s) whose individuals are fetched"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "selectGraph",
                        "type": "boolean",
                        "required": false,
                        "description": "Bind each triple's named graph"
                    },
                    {
                        "name": "limit",
                        "type": "number",
                        "required": false,
                        "description": "Result limit (defaults to `Config.queryLimit`)"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `subject`, `node`, `subjectLabel` (optional), `nodeLabel` (optional), `g` (optional)."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "access": "read"
    },
    {
        "name": "getNodesTypes",
        "module": "Sparql_OWL",
        "description": "Returns the `rdf:type` triples (with their graph) for given resources, batching the ids into slices of 200 and concatenating results (`GRAPH ?g { ?subject rdf:type ?type }`). Historical: no remaining caller in the codebase (kept for backward compatibility / plugins). Note: its body references an undeclared `options` variable, so it would currently throw if called.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name whose endpoint is queried"
            },
            {
                "name": "ids",
                "type": "(string|string[])",
                "required": true,
                "description": "Resource URI(s) whose types are fetched"
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `subject`, `type`, and `g`."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null,
        "summary": "Returns the `rdf:type` triples (with their graph) for given resources, batching the ids into slices of 200 and concatenating results (`GRAPH ?g { ?subject rdf:type ?type }`)."
    },
    {
        "name": "getNodesOwlTypeMap",
        "module": "Sparql_OWL",
        "description": "Returns a map of each resource URI to a simplified OWL type — `\"NamedIndividual\"`, `\"Class\"` or `\"Restriction\"` — derived from its concatenated `rdf:type` values, batching the ids into slices of 200. Live: used by `Lineage_createRelation` to determine a node's OWL kind. The canonical of the three `getNodesType*` helpers (`getNodesTypes`/`getNodesTypesMap` are historical).",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name whose endpoint is queried"
            },
            {
                "name": "ids",
                "type": "(string|string[])",
                "required": true,
                "description": "Resource URI(s) whose OWL type is determined"
            }
        ],
        "returns": {
            "type": "err|Object",
            "description": "Throws an error or returns a map from each resource URI to `NamedIndividual`, `Class`, or `Restriction`."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "summary": "Returns a map of each resource URI to a simplified OWL type — `\"NamedIndividual\"`, `\"Class\"` or `\"Restriction\"` — derived from its concatenated `rdf:type` values, batching the ids into slices of 200.",
        "access": "read"
    },
    {
        "name": "getPropertiesRestrictionsDescription",
        "module": "Sparql_OWL",
        "description": "Returns which classes actually use the given properties, and against which target classes: the reverse of asking a class what it declares. Takes **property** URIs, so a class URI simply returns nothing; get property URIs from {@link module:Sparql_generic.getDistinctPredicates} or {@link module:Sparql_OWL.getObjectPropertiesDomainAndRange}. Matches `?restriction owl:onProperty ?prop`, `?sourceClass <taxonomyPredicate> ?restriction`, plus the constraint value (`owl:someValuesFrom`/`allValuesFrom`/`hasValue`/ cardinality) as `?targetClass`. Missing labels are filled afterwards.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name to query"
            },
            {
                "name": "propIds",
                "type": "(string|string[])",
                "required": true,
                "description": "Property URI(s) whose restrictions are described"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "subPropIds",
                        "type": "(string|string[])",
                        "required": false,
                        "description": "Filter by sub-property URI(s) instead of `propIds`"
                    },
                    {
                        "name": "filter",
                        "type": "string",
                        "required": false,
                        "description": "Extra SPARQL filter (replaces the property filter)"
                    },
                    {
                        "name": "selectGraph",
                        "type": "boolean",
                        "required": false,
                        "description": "Bind each triple's named graph"
                    },
                    {
                        "name": "limit",
                        "type": "number",
                        "required": false,
                        "description": "Result limit (defaults to `Config.queryLimit`)"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `restriction`, `prop`, `sourceClass`, `constraintType` (optional), `targetClass` (optional)."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "summary": "Returns which classes actually use the given properties, and against which target classes: the reverse of asking a class what it declares. Takes **property** URIs, so a class URI simply returns nothing; get property URIs from {@link module:Sparql_generic.getDistinctPredicates} or {@link module:Sparql_OWL.getObjectPropertiesDomainAndRange}.",
        "access": "read",
        "mcpTool": "sls_property_usage"
    },
    {
        "name": "getUrisLabelsMap",
        "module": "Sparql_OWL",
        "description": "Returns a map of URI → `rdfs:label` for given URIs (`?s rdfs:label ?sLabel`), falling back to the URI's local name for any URI without a label. Deliberately not `@expose`d: {@link module:Sparql_OWL.getLabelsMap} is the API-facing variant. It paginates instead of capping at 10000 rows and accepts `options.lang`, which this one lacks — on a multilingual source each URI here keeps whichever label comes last. A URI list is expressed against `getLabelsMap` through `options.filter`, as `Sparql_generic` already does.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name to query"
            },
            {
                "name": "uris",
                "type": "string[]",
                "required": true,
                "description": "URIs whose labels are fetched"
            }
        ],
        "returns": {
            "type": "err|Object",
            "description": "Throws an error or returns a map from each requested URI to its label."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null,
        "summary": "Returns a map of URI → `rdfs:label` for given URIs (`?s rdfs:label ?sLabel`), falling back to the URI's local name for any URI without a label."
    },
    {
        "name": "getLabelsMap",
        "module": "Sparql_OWL",
        "description": "Returns a map of URI → `rdfs:label` for a source, paginating through results in pages of 2000 (`async.whilst` over offset) until `Config.queryLimit` is reached. URIs without a label fall back to their local name. Returns `{}` immediately when `options.noExecute`.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name to query"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "noExecute",
                        "type": "boolean",
                        "required": false,
                        "description": "Skip the query and return an empty map"
                    },
                    {
                        "name": "lang",
                        "type": "string",
                        "required": false,
                        "description": "Restrict labels to this language (or untagged)"
                    },
                    {
                        "name": "filter",
                        "type": "string",
                        "required": false,
                        "description": "Extra SPARQL filter appended to the query"
                    },
                    {
                        "name": "selectGraph",
                        "type": "boolean",
                        "required": false,
                        "description": "Bind each triple's named graph"
                    },
                    {
                        "name": "withoutImports",
                        "type": "boolean",
                        "required": false,
                        "description": "Exclude imported graphs from the `FROM` clause"
                    },
                    {
                        "name": "processorFectchSize",
                        "type": "number",
                        "required": false,
                        "description": "Page size for pagination (defaults to 2000)"
                    },
                    {
                        "name": "limit",
                        "type": "number",
                        "required": false,
                        "description": "Total result cap (defaults to `Config.queryLimit`)"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Object",
            "description": "Throws an error or returns a map from each source URI to its label."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "access": "read"
    },
    {
        "name": "getDictionary",
        "module": "Sparql_OWL",
        "description": "Returns the dictionary of a source — the list of its resources as `{id, label}` (plus the graph when `selectGraph`) — paginating in chunks of 2000 with `async.whilst`. Each page is either passed to a `processor` callback (for streaming) or accumulated and returned at the end. Supports type/label/blank-node filters and skos:prefLabel. Primary use across the app is id→label retrieval: building selection lists (`promptedSelectWidget`), label maps for models (`KGquery_graph`), and search/standardizer lookups. Callers fall back to {@link module:Sparql_common.getLabelFromURI} when `label` is absent.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name to query"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "lang",
                        "type": "string",
                        "required": false,
                        "description": "Restrict labels to this language (or untagged)"
                    },
                    {
                        "name": "type",
                        "type": "string",
                        "required": false,
                        "description": "Restrict to resources of this `rdf:type`"
                    },
                    {
                        "name": "filter",
                        "type": "string",
                        "required": false,
                        "description": "Extra SPARQL filter appended to the query"
                    },
                    {
                        "name": "selectGraph",
                        "type": "boolean",
                        "required": false,
                        "description": "Bind each triple's named graph"
                    },
                    {
                        "name": "includeBlankNodes",
                        "type": "boolean",
                        "required": false,
                        "description": "Include blank-node ids"
                    },
                    {
                        "name": "withoutImports",
                        "type": "boolean",
                        "required": false,
                        "description": "Exclude imported graphs from the `FROM` clause"
                    },
                    {
                        "name": "skosPrefLabel",
                        "type": "boolean",
                        "required": false,
                        "description": "Also fetch `skos:prefLabel`"
                    },
                    {
                        "name": "indexedPredicates",
                        "type": "string[]",
                        "required": false,
                        "description": "Extra predicates fetched for indexation, see {@link module:Sparql_common.getIndexedPredicatesClauses}"
                    },
                    {
                        "name": "processorFectchSize",
                        "type": "number",
                        "required": false,
                        "description": "Page size (defaults to 2000)"
                    },
                    {
                        "name": "limit",
                        "type": "number",
                        "required": false,
                        "description": "Total result cap (defaults to `Config.queryLimit`)"
                    }
                ]
            },
            {
                "name": "processor",
                "type": "Function",
                "required": false,
                "description": "Optional `(pageBindings, cb)` processor invoked per page; if omitted, pages are accumulated"
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns accumulated SPARQL results with variables: `id`, `type` (optional), `label` (optional)."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "summary": "Returns the dictionary of a source — the list of its resources as `{id, label}` (plus the graph when `selectGraph`) — paginating in chunks of 2000 with `async.whilst`. Each page is either passed to a `processor` callback (for streaming) or accumulated and returned at the end. Supports type/label/blank-node filters and skos:prefLabel.",
        "access": "read"
    },
    {
        "name": "getObjectProperties",
        "module": "Sparql_OWL",
        "description": "Lists all `owl:ObjectProperty` of a source with their labels (`?property rdf:type owl:ObjectProperty` + language-filtered label).",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name to query"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "withGraph",
                        "type": "boolean",
                        "required": false,
                        "description": "Wrap in `GRAPH ?g { ... }` and bind the graph"
                    },
                    {
                        "name": "withoutImports",
                        "type": "boolean",
                        "required": false,
                        "description": "Exclude imported graphs from the `FROM` clause"
                    },
                    {
                        "name": "skosLabels",
                        "type": "boolean",
                        "required": false,
                        "description": "Also match `skos:prefLabel` for labels"
                    },
                    {
                        "name": "filter",
                        "type": "string",
                        "required": false,
                        "description": "Extra SPARQL filter appended to the query"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `property`, `propertyLabel`, `g` (optional)."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "access": "read"
    },
    {
        "name": "generateInverseRestrictions",
        "module": "Sparql_OWL",
        "description": "Materialises the inverse of every restriction using a given property: fetches the restrictions on `propId` via {@link module:Sparql_OWL.getObjectRestrictions}, builds the inverse restriction triples (swapping subject/value, using `inversePropId`) and inserts them in batches of 200.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name whose graph is edited"
            },
            {
                "name": "propId",
                "type": "string",
                "required": true,
                "description": "Property URI whose restrictions are inverted"
            },
            {
                "name": "inversePropId",
                "type": "string",
                "required": true,
                "description": "Property URI used in the generated inverse restrictions"
            },
            {
                "name": "cardinality",
                "type": "number",
                "required": true,
                "description": "Cardinality for the generated restrictions"
            }
        ],
        "returns": {
            "type": "err|number",
            "description": "Throws an error or returns the number of generated triples inserted."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "getPropertiesInheritedConstraints",
        "module": "Sparql_OWL",
        "description": "Returns, for each given property, the domain/range inherited from its super-properties (`?prop0 rdfs:subPropertyOf+ ?prop` where `?prop` carries `rdfs:domain`/`rdfs:range`), as a map keyed by the original property.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name to query"
            },
            {
                "name": "properties",
                "type": "string[]",
                "required": true,
                "description": "Property URIs whose inherited constraints are fetched"
            },
            {
                "name": "options",
                "type": "Object",
                "required": true,
                "description": "Query options",
                "properties": [
                    {
                        "name": "withGraph",
                        "type": "boolean",
                        "required": false,
                        "description": "Bind the graph in the `FROM` clause"
                    },
                    {
                        "name": "withoutImports",
                        "type": "boolean",
                        "required": false,
                        "description": "Exclude imported graphs from the `FROM` clause"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Object",
            "description": "Throws an error or returns a map keyed by property URI with fields: `parentProp`, `prop`, `domain` (optional), `range` (optional), `domainLabel` (optional), `rangeLabel` (optional)."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "access": "read"
    },
    {
        "name": "getInferredPropertiesDomainsAndRanges",
        "module": "Sparql_OWL",
        "description": "Returns all object properties of a source with their inferred domains, ranges, sub-properties and inverse properties (and their labels), assembled from a multi-`UNION` query and folded into a per-property map. Domains/ranges of inverse properties are also covered. Example entry: ```json \"…/hasArrangedPart\": { \"prop\": \"…/hasArrangedPart\", \"propLabel\": \"hasArrangedPart\", \"subProps\": [\"…/hasAssembledPart\", \"…/hasFeature\"], \"domain\": \"…/PhysicalObject\", \"domainLabel\": \"PhysicalObject\", \"range\": \"…/xx\", \"rangeLabel\": \"xx\", \"inverseProp\": \"…/arrangedPartOf\", \"inversePropLabel\": \"arrangedPartOf\" } ```",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name to query"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "prefLabelAlso",
                        "type": "boolean",
                        "required": false,
                        "description": "Match `skos:prefLabel` in addition to `rdfs:label`"
                    },
                    {
                        "name": "filter",
                        "type": "string",
                        "required": false,
                        "description": "Extra SPARQL filter applied within each UNION branch"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Object",
            "description": "Throws an error or returns a map keyed by property URI with fields: `prop`, `propLabel`, `subProps`, `domain` (optional), `domainLabel` (optional), `range` (optional), `rangeLabel` (optional), `inverseProp` (optional), `inversePropLabel` (optional)."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "summary": "Returns all object properties of a source with their inferred domains, ranges, sub-properties and inverse properties (and their labels), assembled from a multi-`UNION` query and folded into a per-property map. Domains/ranges of inverse properties are also covered.",
        "access": "read",
        "mcpTool": "sls_property_schema"
    },
    {
        "name": "getAllTriples",
        "module": "Sparql_OWL",
        "description": "Returns all triples of a source (or explicit graph) with subject/predicate/object labels, optionally restricted to triples where a given role (subject/predicate/object) is one of `ids` (batched into `Sparql_generic.slicesSize` slices). Blank-node objects can be excluded.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name to query (or null with `options.graphUri`)"
            },
            {
                "name": "role",
                "type": "string",
                "required": true,
                "description": "Variable (`subject`/`predicate`/`object`) the `ids` filter applies to"
            },
            {
                "name": "ids",
                "type": "(string|string[])",
                "required": true,
                "description": "URI(s) restricting the chosen role"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "source",
                        "type": "string",
                        "required": false,
                        "description": "Override source (sets `graphUri` from it)"
                    },
                    {
                        "name": "graphUri",
                        "type": "string",
                        "required": false,
                        "description": "Explicit graph URI when no source is given"
                    },
                    {
                        "name": "removeBlankNodesObjects",
                        "type": "boolean",
                        "required": false,
                        "description": "Exclude blank-node objects"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `subject`, `predicate`, `object`, `subjectLabel` (optional), `predicateLabel` (optional), `objectLabel` (optional)."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "generateOWL",
        "module": "Sparql_OWL",
        "description": "Serialises a source to Turtle by paginating `DESCRIBE ?s ?p ?o` queries (`text/turtle` Accept header) in pages of 1000, separating `@prefix` lines from the body and concatenating everything into a single Turtle document. Not exposed: its only caller, lineage_sources.js's exportOWL, is itself never invoked from any UI element, and this crashes outright through the MCP/RemoteCodeRunner path (`result.result` is undefined there for a non-JSON Accept header). Unused and non-functional outside a real browser.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name to serialise"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "filter",
                        "type": "string",
                        "required": false,
                        "description": "Reserved filter (currently unused in the query body)"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|string",
            "description": "Throws an error or returns the source serialized as a Turtle string."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null,
        "summary": "Serialises a source to Turtle by paginating `DESCRIBE ?s ?p ?o` queries (`text/turtle` Accept header) in pages of 1000, separating `@prefix` lines from the body and concatenating everything into a single Turtle document."
    },
    {
        "name": "getDistinctClassLabels",
        "module": "Sparql_OWL",
        "description": "Returns the distinct labels (or another property's values) of instances of given classes (`?id rdf:type ?type` filtered by `classIds`, then `?id <labelProperty> ?label`).",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name to query"
            },
            {
                "name": "classIds",
                "type": "(string|string[])",
                "required": true,
                "description": "Class URI(s) whose instances' labels are fetched"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "otherProperty",
                        "type": "string",
                        "required": false,
                        "description": "Use this property instead of `rdfs:label`"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `label`, `id` (optional)."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "getIndividualsType",
        "module": "Sparql_OWL",
        "description": "Returns the (non-`owl:NamedIndividual`) `rdf:type`(s) of given individuals, batching the ids into slices of 50.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name to query"
            },
            {
                "name": "allIds",
                "type": "string[]",
                "required": true,
                "description": "Individual URIs whose class types are fetched"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Reserved options object"
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variable: `type`."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "access": "read"
    },
    {
        "name": "getIndividualsOfClass",
        "module": "Sparql_OWL",
        "description": "Returns the `owl:NamedIndividual` instances of given classes with their labels (`?id rdf:type ?type` filtered by `classIds` + `?id rdf:type owl:NamedIndividual`), batching the class ids into slices of 50.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name to query"
            },
            {
                "name": "classIds",
                "type": "string[]",
                "required": true,
                "description": "Class URIs whose individuals are fetched"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Reserved options object"
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `id` and `label`."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "access": "read"
    },
    {
        "name": "clearGraph",
        "module": "Sparql_OWL",
        "description": "Empties a named graph by POSTing to the backend `/kg/clearGraph` endpoint (not a SPARQL query).",
        "params": [
            {
                "name": "graphUri",
                "type": "string",
                "required": true,
                "description": "URI of the graph to clear"
            }
        ],
        "returns": {
            "type": "err|undefined",
            "description": "Throws an error or completes without a result after clearing the graph."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "copyUriTriplesFromSourceToSource",
        "module": "Sparql_OWL",
        "description": "Intended to copy a resource's triples from one source to another. Currently fetches the resource's triples via {@link module:Sparql_OWL.getNodeInfos} and builds the triple list, but does not yet insert them (incomplete).",
        "params": [
            {
                "name": "fromSourceLabel",
                "type": "any",
                "required": true,
                "description": ""
            },
            {
                "name": "toSourceLabel",
                "type": "any",
                "required": true,
                "description": ""
            },
            {
                "name": "subjectUri",
                "type": "string",
                "required": true,
                "description": "URI of the resource to copy"
            }
        ],
        "returns": {
            "type": "err|undefined",
            "description": "Throws an error or completes without a result; returns `\"nothing to copy\"` as an error when no triples exist."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "getAllDescendants",
        "module": "Sparql_OWL",
        "description": "Returns all descendants of given resources via a configurable taxonomy predicate, with each descendant's direct parent and labels. The path operator adapts to options (`+` default, `*` to include the parent, `{0,depth}` for bounded depth), and results are paginated in pages of 2000 with `async.whilst`. Deliberately not `@expose`d: {@link module:Sparql_OWL.getNodesAncestorsOrDescendants} is the API-facing variant. It covers both traversal directions through a single boolean instead of requiring the caller to spell out a taxonomy predicate, and it returns the hierarchy chains already rebuilt per input class. This one also drops the paging error (its final `async.whilst` callback ignores `err`), so a partial result cannot be told from a complete one — unacceptable on a public API.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name to query"
            },
            {
                "name": "resourcesIds",
                "type": "(string|string[])",
                "required": false,
                "description": "Ancestor resource URI(s) whose descendants are fetched"
            },
            {
                "name": "taxonomyPredicate",
                "type": "string",
                "required": false,
                "description": "=\"rdfs:subClassOf\"] - Predicate defining the parent relation"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "includeParent",
                        "type": "boolean",
                        "required": false,
                        "description": "Include the starting resource(s) (`*` path)"
                    },
                    {
                        "name": "depth",
                        "type": "number",
                        "required": false,
                        "description": "Limit traversal to this depth (`{0,depth}` path)"
                    },
                    {
                        "name": "filter",
                        "type": "string",
                        "required": false,
                        "description": "Extra SPARQL filter appended to the query"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `descendant`, `descendantParent`, `descendantLabel` (optional), `descendantParentLabel` (optional)."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null,
        "summary": "Returns all descendants of given resources via a configurable taxonomy predicate, with each descendant's direct parent and labels. The path operator adapts to options (`+` default, `*` to include the parent, `{0,depth}` for bounded depth), and results are paginated in pages of 2000 with `async.whilst`."
    },
    {
        "name": "getGraphsWithSameClasses",
        "module": "Sparql_OWL",
        "description": "Finds the named graphs that share subjects with a source's graph: an inner `SELECT ?sub` over the source graph feeds an outer `GRAPH ?g { ?sub ?p ?obj }`, returning the distinct graphs containing those subjects. Historical: no remaining caller in the codebase (kept for backward compatibility / plugins).",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name whose subjects anchor the search"
            },
            {
                "name": "filter",
                "type": "string",
                "required": true,
                "description": "SPARQL filter restricting the inner subjects"
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variable: `g`."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null,
        "summary": "Finds the named graphs that share subjects with a source's graph: an inner `SELECT ?sub` over the source graph feeds an outer `GRAPH ?g { ?sub ?p ?obj }`, returning the distinct graphs containing those subjects."
    },
    {
        "name": "createSkgFromOntology",
        "module": "Sparql_OWL",
        "description": "Generates a Semantic Knowledge Graph (SKG) from an OWL ontology into a target graph, in a sequence of INSERT queries: turning `rdfs:subClassOf` into `rdf:type` (classes become `owl:NamedIndividual`) and turning OWL restrictions (`owl:onProperty` + `owl:someValuesFrom`) into direct predicates between individuals.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name to transform"
            },
            {
                "name": "skgGraphUri",
                "type": "string",
                "required": true,
                "description": "Target graph URI receiving the generated SKG triples"
            }
        ],
        "returns": {
            "type": "err|undefined",
            "description": "Throws an error or completes without a result after generating the SKG triples."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "getNodeLabel",
        "module": "Sparql_OWL",
        "description": "Returns the label and RDF type of one resource.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name to query"
            },
            {
                "name": "id",
                "type": "string",
                "required": true,
                "description": "URI of the resource whose label is fetched"
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `subject`, `type`, and `subjectLabel`."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "getNodesSuperClassesAndDefinition",
        "module": "Sparql_OWL",
        "description": "Returns, for each given node, its label, textual definition (`dcterms:description`| `rdfs:isDefinedBy`|`skos:definition`) and up to four levels of OWL super-classes with their labels. The cheapest way to confirm you picked the right sense of a term before grounding an answer on it. OWL sources only. Built by chaining nested `OPTIONAL` blocks on `rdfs:subClassOf` (each super-class is restricted to `rdf:type owl:Class`). Used by `Lineage_selection.exportCsv` to dump the whiteboard classes and their hierarchy/definition as a tab-separated file.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "OWL source name to query"
            },
            {
                "name": "nodeIds",
                "type": "(string|string[])",
                "required": true,
                "description": "Node URI(s) to describe (matched on `?node`)"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "returnQueryStr",
                        "type": "boolean",
                        "required": false,
                        "description": "Return the SPARQL query string instead of executing it"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `node`, `node_label`, `definition` (optional), `superClass1Label` (optional), `superClass2Label` (optional), `superClass3Label` (optional), `superClass4Label` (optional), `description` (optional), `propLabel` (optional), `targetClass_label` (optional)."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "summary": "Returns, for each given node, its label, textual definition (`dcterms:description`| `rdfs:isDefinedBy`|`skos:definition`) and up to four levels of OWL super-classes with their labels. The cheapest way to confirm you picked the right sense of a term before grounding an answer on it. OWL sources only.",
        "access": "read",
        "mcpTool": "sls_node_definition"
    },
    {
        "name": "getClassesWithoutTaxonomy",
        "module": "Sparql_OWL",
        "description": "Retrieves all OWL classes of a source that has no taxonomy (no rdfs:subClassOf hierarchy), attaching each class to owl:Thing so flat ontologies can still be indexed and searched. Used as a fallback by SearchUtil.generateElasticIndex when getSourceTaxonomy returns no class.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name whose classes are retrieved"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Build options",
                "properties": [
                    {
                        "name": "filter",
                        "type": "string",
                        "required": false,
                        "description": "Extra SPARQL clause appended to the query"
                    },
                    {
                        "name": "withoutImports",
                        "type": "boolean",
                        "required": false,
                        "description": "Exclude imported graphs from the `FROM` clause"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Object",
            "description": "Returns `{classesMap, labels}` through the callback; empty map for non-OWL sources."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "access": "read"
    },
    {
        "name": "getTopConcepts",
        "module": "Sparql_SKOS",
        "description": "Returns the top concepts of a SKOS source (concept-scheme roots). Runs `select distinct ?topConcept ?topConceptLabel ... WHERE { <topConceptFilter> ?topConcept prefLabel ?topConceptLabel. }` ordered by label, optionally restricting to a language and to concepts reachable from given collections.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "SKOS source name to query"
            },
            {
                "name": "options",
                "type": "Object",
                "required": true,
                "description": "Query options",
                "properties": [
                    {
                        "name": "noLang",
                        "type": "boolean",
                        "required": false,
                        "description": "Skip the preferred-language filter"
                    },
                    {
                        "name": "filterCollections",
                        "type": "(string|string[])",
                        "required": false,
                        "description": "Collection URI(s) the top concepts must belong to"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `topConcept`, `topConceptLabel`."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "getNodeChildren",
        "module": "Sparql_SKOS",
        "description": "Returns the children of node(s) down to a given depth in a SKOS hierarchy. Builds nested `OPTIONAL` blocks chaining `?childN+1 skos:broader ?childN` (up to `optionalDepth`), each fetching the child's preferred label (optionally language-filtered) and `rdf:type`. The `ids`/`words` parent filter is required. Example (collection-filtered children): ```sparql select distinct ?child1 ?child1Label ?subjectLabel FROM <http://souslesens/thesaurus/TEST/> WHERE { ?child1 skos:broader ?subject. ?subject skos:prefLabel ?subjectLabel. OPTIONAL { ?child1 skos:prefLabel ?child1Label. } filter(?subject = <...>) } ORDER BY ?child1Label limit 1000 ```",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "SKOS source name to query"
            },
            {
                "name": "words",
                "type": "(string|string[])",
                "required": true,
                "description": "Label word(s) identifying the parent; mutually exclusive with `ids`"
            },
            {
                "name": "ids",
                "type": "(string|string[])",
                "required": true,
                "description": "Parent concept URI(s) whose children are fetched"
            },
            {
                "name": "descendantsDepth",
                "type": "number",
                "required": true,
                "description": "Number of `skos:broader` levels to expand (capped at `optionalDepth`)"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "noLang",
                        "type": "boolean",
                        "required": false,
                        "description": "Skip the language filter on labels"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `subject`, `subjectLabel` (optional), `child1`, `child1Label` (optional), `child1Type` (optional), depth-indexed `childN` (optional), `childNLabel` (optional), `childNType` (optional); with `options.filterCollections`: `collection`, `acollection`, `collectionLabel`, `acollectionLabel`."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null,
        "summary": "Returns the children of node(s) down to a given depth in a SKOS hierarchy. Builds nested `OPTIONAL` blocks chaining `?childN+1 skos:broader ?childN` (up to `optionalDepth`), each fetching the child's preferred label (optionally language-filtered) and `rdf:type`. The `ids`/`words` parent filter is required."
    },
    {
        "name": "getCollectionNodes",
        "module": "Sparql_SKOS",
        "description": "Returns the concepts belonging to a SKOS collection (and their `skos:broader` links). With no collection, lists all non-collection subjects; with a collection URI, walks `?collection skos:member* ?acollection` to the target collection and returns its members' concepts with their preferred labels (filtered to English).",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "SKOS source name to query"
            },
            {
                "name": "collection",
                "type": "string",
                "required": true,
                "description": "Collection URI whose member concepts are fetched (empty = all concepts)"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "filter",
                        "type": "Object",
                        "required": false,
                        "description": "`{predicates}` to restrict the predicates returned"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `subject`, `predicate`, `object`, `type`; with a collection: `collection`, `acollection`, `collectionLabel`, `acollectionLabel`, `subjectLabel`."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "getNodeParents",
        "module": "Sparql_SKOS",
        "description": "Returns the ancestors of node(s) up to a given depth in a SKOS hierarchy. Builds nested `OPTIONAL` blocks chaining `?broaderN-1 skos:broader ?broaderN` (up to `optionalDepth`), each fetching the broader concept's preferred label and type. Can search alt labels and exclude collection-only branches.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "SKOS source name to query"
            },
            {
                "name": "words",
                "type": "(string|string[])",
                "required": true,
                "description": "Label word(s) identifying the start node; mutually exclusive with `ids`"
            },
            {
                "name": "ids",
                "type": "(string|string[])",
                "required": true,
                "description": "Start concept URI(s) whose ancestors are fetched"
            },
            {
                "name": "ancestorsDepth",
                "type": "number",
                "required": true,
                "description": "Number of `skos:broader` levels to climb (capped at `optionalDepth`)"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "noLang",
                        "type": "boolean",
                        "required": false,
                        "description": "Skip the language filter on labels"
                    },
                    {
                        "name": "searchAltLabels",
                        "type": "boolean",
                        "required": false,
                        "description": "Also match `skos:altLabel` on the start node"
                    },
                    {
                        "name": "filterCollections",
                        "type": "(string|string[])",
                        "required": false,
                        "description": "Exclude ancestors that are members of these collections"
                    },
                    {
                        "name": "limit",
                        "type": "number",
                        "required": false,
                        "description": "Result limit (defaults to `Config.queryLimit`)"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `subject`, `subjectLabel` (optional), `type` (optional), `broader1` (optional), `broader1Label` (optional), `broader1Type` (optional), depth-indexed `broaderN` (optional), `broaderNLabel` (optional), `broaderNType` (optional)."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "getNodeInfos",
        "module": "Sparql_SKOS",
        "description": "Returns all triples (`?id ?prop ?value`) describing a concept. Delegates to a non-SKOS controller when the source's `controllerName` is not `Sparql_SKOS`. Can also fetch value/property labels and restrict to a given property.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name to query"
            },
            {
                "name": "conceptId",
                "type": "(string|string[])",
                "required": true,
                "description": "Concept URI(s) to describe"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "propertyFilter",
                        "type": "(string|string[])",
                        "required": false,
                        "description": "Restrict to these property URIs"
                    },
                    {
                        "name": "getValuesLabels",
                        "type": "boolean",
                        "required": false,
                        "description": "Also fetch `skos:prefLabel` of values and properties"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `id`, `prop`, `value`, `valueLabel` (optional), `propLabel` (optional)."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "getItems",
        "module": "Sparql_SKOS",
        "description": "Returns all concept items of a SKOS source with their preferred labels. Delegates to a non-SKOS controller when applicable. Supports an extra filter, a language filter, and restriction to members of given collections.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name to query"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Query options",
                "properties": [
                    {
                        "name": "filter",
                        "type": "string",
                        "required": false,
                        "description": "Extra SPARQL pattern appended to the WHERE clause"
                    },
                    {
                        "name": "filterCollections",
                        "type": "(string|string[])",
                        "required": false,
                        "description": "Collection URI(s) the items must belong to"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `subject`, `x`, `y`, `subjectLabel` (optional)."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "getSingleNodeAllGenealogy",
        "module": "Sparql_SKOS",
        "description": "Returns the full ancestor chain of a concept via the transitive closure `?subject skos:broader* ?broader`, fetching each broader concept's label and type. Falls back to top concepts for non-SKOS sources.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name to query"
            },
            {
                "name": "id",
                "type": "string",
                "required": true,
                "description": "Concept URI whose ancestors are fetched"
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `subject`, `broader`, `broaderLabel`, `type`."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "getSingleNodeAllDescendants",
        "module": "Sparql_SKOS",
        "description": "Returns the full set of descendants of a concept via the transitive closure over inverse broader / narrower predicates (`?subject ^skos:broader*|skos:narrower* ?narrower`), fetching each descendant's label and type.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name to query"
            },
            {
                "name": "id",
                "type": "string",
                "required": true,
                "description": "Concept URI whose descendants are fetched"
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `subject`, `narrower`, `narrowerLabel`, `type`."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "getNodeLabel",
        "module": "Sparql_SKOS",
        "description": "Fetches the preferred label and `rdf:type` of a single concept, optionally language-filtered.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name to query"
            },
            {
                "name": "id",
                "type": "string",
                "required": true,
                "description": "Concept URI whose label is fetched"
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `subject`, `type`, `subjectLabel`."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "getNodesAllTriples",
        "module": "Sparql_SKOS",
        "description": "Fetches every outgoing triple for a list of subjects, batching the ids into slices of 2000 (`?subject ?prop ?value. FILTER(?subject in (...))`) and concatenating results.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name to query"
            },
            {
                "name": "subjectIds",
                "type": "string[]",
                "required": true,
                "description": "Subject URIs whose triples are fetched"
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `subject`, `prop`, `value`."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "deleteTriples",
        "module": "Sparql_SKOS",
        "description": "Deletes triples matching a subject/predicate/object pattern from the source's named graph (`with <graphUri> DELETE { ?s ?p ?o } WHERE { ?s ?p ?o <filters> }`), each component contributing a `Sparql_common.getUriFilter` clause.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name whose graph is edited"
            },
            {
                "name": "subjectUri",
                "type": "string",
                "required": true,
                "description": "Subject URI to match (optional)"
            },
            {
                "name": "predicateUri",
                "type": "string",
                "required": true,
                "description": "Predicate URI to match (optional)"
            },
            {
                "name": "objectUri",
                "type": "string",
                "required": true,
                "description": "Object URI to match (optional)"
            }
        ],
        "returns": {
            "type": "err|*",
            "description": "Throws an error or returns the callback result."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "triplesObjectToString",
        "module": "Sparql_SKOS",
        "description": "Serialises a triple descriptor into a SPARQL triple string: URI objects are angle-bracketed, literals are single-quoted with an optional `@lang` tag. An inverse predicate (prefixed with `^`) swaps subject and object.",
        "params": [
            {
                "name": "item",
                "type": "Object",
                "required": true,
                "description": "Triple descriptor with `subject`, `predicate`, `object`, `valueType`, and optional `sourceVariables.lang`"
            }
        ],
        "returns": {
            "type": "string",
            "description": "A single `subject predicate object . ` SPARQL triple string"
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "insertTriples",
        "module": "Sparql_SKOS",
        "description": "Inserts triples into the source's named graph as a single `WITH GRAPH <graphUri> INSERT DATA { ... }` statement, serialising each triple via {@link module:Sparql_SKOS.triplesObjectToString}.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name whose graph receives the triples"
            },
            {
                "name": "triples",
                "type": "Object[]",
                "required": true,
                "description": "Triple descriptor objects to insert"
            }
        ],
        "returns": {
            "type": "err|*",
            "description": "Throws an error or returns the callback result."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "update",
        "module": "Sparql_SKOS",
        "description": "Replaces all triples of a subject in the source's graph: deletes every `?s ?p ?o` for the subject, then inserts the supplied triples, in one combined `WITH GRAPH ... DELETE ... ; INSERT DATA ...` statement. The subject is taken from the first triple.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name whose graph is edited"
            },
            {
                "name": "triples",
                "type": "Object[]",
                "required": true,
                "description": "Replacement triple descriptors (all sharing the same subject)"
            }
        ],
        "returns": {
            "type": "err|*",
            "description": "Throws an error or returns the callback result."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "deleteGraph",
        "module": "Sparql_SKOS",
        "description": "Empties the source's named graph by deleting all its triples (`WITH <graphUri> DELETE { ?s ?p ?o }`).",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name whose graph is emptied"
            }
        ],
        "returns": {
            "type": "err|*",
            "description": "Throws an error or returns the callback result."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "copyGraph",
        "module": "Sparql_SKOS",
        "description": "Copies a source's entire named graph into another graph using a single SPARQL `COPY <fromGraph> TO <toGraph>` statement.",
        "params": [
            {
                "name": "fromSourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name whose graph is the copy origin"
            },
            {
                "name": "toGraphUri",
                "type": "string",
                "required": true,
                "description": "Destination graph URI"
            }
        ],
        "returns": {
            "type": "err|*",
            "description": "Throws an error or returns the callback result."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "setBindingsOptionalProperties",
        "module": "Sparql_SKOS",
        "description": "Backfills missing `<field>Type` and `<field>Label` cells for a family of indexed fields (`field`, `field1` … up to the first gap, max 20) in result bindings. Defaults the type to `skos:Concept` (or `options.type`) and derives labels from the URI's local name.",
        "params": [
            {
                "name": "bindings",
                "type": "Array<Object>",
                "required": true,
                "description": "Result bindings to enrich in place"
            },
            {
                "name": "_field",
                "type": "string",
                "required": true,
                "description": "Base variable name; indexed variants are processed until the first missing one"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Enrichment options",
                "properties": [
                    {
                        "name": "type",
                        "type": "string",
                        "required": false,
                        "description": "Type URI to use instead of the default `skos:Concept`"
                    }
                ]
            }
        ],
        "returns": {
            "type": "Array<Object>",
            "description": "The same bindings with type/label cells filled in"
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "getSourceLangsList",
        "module": "Sparql_SKOS",
        "description": "Lists the distinct languages used in a source's `skos:prefLabel` literals, via `select distinct ?language WHERE { ?p skos:prefLabel ?o. BIND(LANG(?o) AS ?language) }`.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name to query"
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns an array of language codes."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "getSourceVariables",
        "module": "Sparql_generic",
        "description": "Builds and memoizes a per-source bundle of SPARQL building blocks (prefixes string, `FROM` clause, hierarchy predicates, preferred-label predicate, language, limit, endpoint URL). For SKOS sources, defaults come from `Sparql_SKOS.defaultPredicates`, overridden by the source's own `predicates` config.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name to compute query variables for"
            }
        ],
        "returns": {
            "type": "Object",
            "description": "Cached object with `prefixesStr`, `fromStr`, `broaderPredicate`, `narrowerPredicate`, `prefLabelPredicate`, `topConceptFilter`, `lang`, `limit`, `optionalDepth`, `url`, `queryOptions`, `graphUri`"
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "getTopConcepts",
        "module": "Sparql_generic",
        "description": "Returns the roots of a source's class or concept hierarchy by delegating to its controller (`Sparql_OWL` or `Sparql_SKOS`), so it works on an OWL and on a SKOS source alike. The roots are those of the source alone: what it imports is left out of the query, so a class whose only parent lives in an upper ontology such as BFO or IOF is reported as a root here. Ask for the source that holds them to get the roots of the imported ontologies themselves.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name to query"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Controller-specific options",
                "properties": [
                    {
                        "name": "skipTopClassFilter",
                        "type": "boolean",
                        "required": false,
                        "description": "Do not apply any top-class filter"
                    },
                    {
                        "name": "filter",
                        "type": "string",
                        "required": false,
                        "description": "Extra SPARQL filter appended after the top-class filter"
                    },
                    {
                        "name": "withoutImports",
                        "type": "boolean",
                        "required": false,
                        "description": "Exclude imported graphs from the `FROM` clause"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns the delegated controller's top-concept bindings."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "access": "read",
        "mcpTool": "sls_top_concepts",
        "mcpFixed": {
            "options.withoutImports": true,
            "options.skipTopClassFilter": false
        }
    },
    {
        "name": "getNodeInfos",
        "module": "Sparql_generic",
        "description": "Returns every property and value describing a node (or nodes) by delegating to the source controller: the first call to make on an unfamiliar resource. Only outgoing triples are returned by default; set `options.inverseProperties` to also get the triples where the node is the object, which is how you discover what points at it.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name to query"
            },
            {
                "name": "conceptId",
                "type": "(string|string[])",
                "required": true,
                "description": "URI(s) of the node(s) to describe"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Controller-specific options",
                "properties": [
                    {
                        "name": "getValuesLabels",
                        "type": "boolean",
                        "required": false,
                        "description": "Add `rdfs:label` of properties and values"
                    },
                    {
                        "name": "inverseProperties",
                        "type": "boolean",
                        "required": false,
                        "description": "Also return triples where the node is the object"
                    },
                    {
                        "name": "noRestrictions",
                        "type": "boolean",
                        "required": false,
                        "description": "Exclude values that are `owl:Restriction`"
                    },
                    {
                        "name": "limit",
                        "type": "number",
                        "required": false,
                        "description": "Result limit"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns the delegated controller's node triple bindings."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "access": "read",
        "mcpTool": "sls_node_infos"
    },
    {
        "name": "getItems",
        "module": "Sparql_generic",
        "description": "Returns all items (concepts/classes) of a source by delegating to its controller.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name to query"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Controller-specific options"
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns the delegated controller's item bindings."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "access": "read"
    },
    {
        "name": "getNodeChildren",
        "module": "Sparql_generic",
        "description": "Returns the direct children of node(s) down to a given depth, delegating to the source controller, so it works on an OWL and on a SKOS source alike. The cheap way to walk a hierarchy one level at a time, where a full descendant closure would be wasteful. Like the ancestor call, `ids` and `words` are mutually exclusive. The filter is split into slices of `self.slicesSize` and queried sequentially with `async.eachSeries`, concatenating the per-slice results. Example controller query (SKOS, with collection filtering): ```sparql select distinct ?child1 ?child1Label ?subjectLabel ?collLabel FROM <http://souslesens/thesaurus/TEST/> WHERE { ?child1 skos:broader ?subject. ?subject skos:prefLabel ?subjectLabel. OPTIONAL { ?child1 skos:prefLabel ?child1Label. } filter(?subject = <...>) } ORDER BY ?child1Label limit 1000 ```",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name to query"
            },
            {
                "name": "words",
                "type": "(string|string[])",
                "required": false,
                "description": "Label word(s) to match; mutually exclusive with `ids`"
            },
            {
                "name": "ids",
                "type": "(string|string[])",
                "required": false,
                "description": "Parent node URI(s) whose children are fetched"
            },
            {
                "name": "descendantsDepth",
                "type": "number",
                "required": true,
                "description": "Depth of descendants to retrieve"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Controller-specific options (merged with `{depth: 0, source}`)"
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns the concatenated children bindings from each slice."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "summary": "Returns the direct children of node(s) down to a given depth, delegating to the source controller, so it works on an OWL and on a SKOS source alike. The cheap way to walk a hierarchy one level at a time, where a full descendant closure would be wasteful. Like the ancestor call, `ids` and `words` are mutually exclusive.",
        "access": "read",
        "mcpTool": "sls_node_children"
    },
    {
        "name": "getNodeParents",
        "module": "Sparql_generic",
        "description": "Returns the superclass or broader chain of node(s) up to a given depth, delegating to the source controller, so it works on an OWL and on a SKOS source alike. Use it to generalise from a node, or to find nodes by label: `ids` and `words` are mutually exclusive, pass `ids` to climb from known URIs and `words` to look up nodes whose label matches, exactly and inside this source only. The filter is sliced into `self.slicesSize` chunks and queried sequentially, concatenating the per-slice results. Returns `[]` when the source has no controller.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name to query"
            },
            {
                "name": "words",
                "type": "(string|string[])",
                "required": false,
                "description": "Label word(s) to match; mutually exclusive with `ids`"
            },
            {
                "name": "ids",
                "type": "(string|string[])",
                "required": false,
                "description": "Child node URI(s) whose ancestors are fetched"
            },
            {
                "name": "ancestorsDepth",
                "type": "number",
                "required": true,
                "description": "Depth of ancestors to retrieve"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Controller-specific options (merged with `{depth: 0, source}`)"
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns the concatenated ancestor bindings from each slice; returns `[]` when the source has no controller."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "summary": "Returns the superclass or broader chain of node(s) up to a given depth, delegating to the source controller, so it works on an OWL and on a SKOS source alike. Use it to generalise from a node, or to find nodes by label: `ids` and `words` are mutually exclusive, pass `ids` to climb from known URIs and `words` to look up nodes whose label matches, exactly and inside this source only.",
        "access": "read",
        "mcpTool": "sls_node_parents"
    },
    {
        "name": "getSingleNodeAllGenealogy",
        "module": "Sparql_generic",
        "description": "Returns the full chain of ancestors of a node. For non-SKOS sources, falls back to the controller's `getTopConcepts`. For SKOS, runs a transitive-closure query (`?subject broaderPredicate* ?broader`) fetching every broader concept's label and type.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name to query"
            },
            {
                "name": "id",
                "type": "string",
                "required": true,
                "description": "URI of the node whose ancestors are fetched"
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `subject`, `broader`, `broaderLabel`, `type`."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "access": "read"
    },
    {
        "name": "getSingleNodeAllDescendants",
        "module": "Sparql_generic",
        "description": "Returns the full set of descendants of a node by running a transitive-closure query over the inverse broader / narrower predicates (`?subject ^broader*|narrower* ?narrower`), fetching each descendant's preferred label and type.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name to query"
            },
            {
                "name": "id",
                "type": "string",
                "required": true,
                "description": "URI of the node whose descendants are fetched"
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `subject`, `narrower`, `narrowerLabel`, `type`."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "access": "read"
    },
    {
        "name": "getNodeLabel",
        "module": "Sparql_generic",
        "description": "Fetches the preferred label and `rdf:type` of a single node, optionally filtered by language. Runs `SELECT * WHERE { ?subject rdf:type ?type. ?subject prefLabel ?subjectLabel. filter(?subject = <id>) }`.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name to query"
            },
            {
                "name": "id",
                "type": "string",
                "required": true,
                "description": "URI of the node whose label is fetched"
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `subject`, `type`, `subjectLabel`."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "access": "read"
    },
    {
        "name": "getEndPointAllGraphsMap",
        "module": "Sparql_generic",
        "description": "Lists all named graphs available on an endpoint by running `SELECT distinct ?g WHERE { GRAPH ?g { ?s ?p ?o } } LIMIT 10000`.",
        "params": [
            {
                "name": "sparqlServerUrl",
                "type": "string",
                "required": false,
                "description": "Endpoint URL to query; defaults to the main server"
            }
        ],
        "returns": {
            "type": "err|Object",
            "description": "Throws an error or returns a map of graph URI to `1`."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "getNodesAllTriples",
        "module": "Sparql_generic",
        "description": "Fetches every outgoing triple (`?subject ?prop ?value`) for a list of subjects, batching the subject ids into slices of 2000 and combining results. Used to bulk-export node data.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name to query"
            },
            {
                "name": "subjectIds",
                "type": "string[]",
                "required": true,
                "description": "Subject URIs whose triples are fetched"
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `subject`, `prop`, `value`."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "access": "read"
    },
    {
        "name": "deleteTriples",
        "module": "Sparql_generic",
        "description": "Deletes triples matching a subject/predicate/object pattern from a source's named graph. Builds `with <graphUri> DELETE { ?s ?p ?o } WHERE { ?s ?p ?o <filters> }`, where each supplied component contributes a `Sparql_common.getUriFilter` clause. Refuses to run when all three filters are empty (would delete the whole graph).",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name whose graph is edited"
            },
            {
                "name": "subjectUri",
                "type": "string",
                "required": true,
                "description": "Subject URI to match (optional)"
            },
            {
                "name": "predicateUri",
                "type": "string",
                "required": true,
                "description": "Predicate URI to match (optional)"
            },
            {
                "name": "object",
                "type": "(string|Object)",
                "required": true,
                "description": "Object to match: a URI/literal string or a literal descriptor (optional)"
            }
        ],
        "returns": {
            "type": "err|*",
            "description": "Throws an error or returns the callback result; early-returns a string error when no filter is supplied."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "access": "write"
    },
    {
        "name": "triplesObjectToString",
        "module": "Sparql_generic",
        "description": "Serialises a triple descriptor object into a SPARQL/Turtle triple string, choosing the right syntax for each element: angle-bracketed URIs and blank nodes, quoted literals with optional `@lang` tag, prefixed names for allowed prefixes, and `^^datatype` typed literals (with numeric and `xsd:dateTime` normalisation). An inverse predicate (prefixed with `^`) swaps subject and object.",
        "params": [
            {
                "name": "item",
                "type": "Object",
                "required": true,
                "description": "Triple descriptor with `subject`/`s`, `predicate`/`p`, `object`/`o`, and optional `lang`, `isString`, `valueType`"
            }
        ],
        "returns": {
            "type": "string",
            "description": "A single `subject predicate object . ` SPARQL triple string"
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "getDefaultSparqlPrefixesStr",
        "module": "Sparql_generic",
        "description": "Builds a `PREFIX key: <uri>` string for every prefix declared in `Config.defaultSparqlPrefixes`, to prepend to INSERT/DELETE queries.",
        "params": [],
        "returns": {
            "type": "string",
            "description": "Concatenated PREFIX declarations for all default prefixes"
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "insertTriples",
        "module": "Sparql_generic",
        "description": "Inserts triples into a source's named graph (or an explicit `options.graphUri`). Each triple descriptor is serialised via {@link module:Sparql_generic.triplesObjectToString}, deduplicated, batched into slices of 200 and run as `WITH GRAPH <graphUri> INSERT { ... }`. With `options.getSparqlOnly`, returns the generated query instead of executing it.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name whose graph receives the triples (may be null if `options.graphUri` is set)"
            },
            {
                "name": "_triples",
                "type": "Object[]",
                "required": true,
                "description": "Triple descriptor objects to insert"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Insert options",
                "properties": [
                    {
                        "name": "graphUri",
                        "type": "string",
                        "required": false,
                        "description": "Target graph URI when no `sourceLabel` is given"
                    },
                    {
                        "name": "sparqlPrefixes",
                        "type": "Object",
                        "required": false,
                        "description": "Prefix map used to expand prefixed names before insertion"
                    },
                    {
                        "name": "getSparqlOnly",
                        "type": "boolean",
                        "required": false,
                        "description": "Return the generated INSERT query instead of executing it"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|*",
            "description": "Throws an error or returns the inserted count (or the query string when `options.getSparqlOnly`)."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "access": "write"
    },
    {
        "name": "deleteTriplesWithFilter",
        "module": "Sparql_generic",
        "description": "Deletes triples from a source's graph matching a SPARQL filter expression (`WITH <graphUri> DELETE { ?s ?x ?y } WHERE { ?s ?x ?y. ?s ?p ?o. filter(<filter>) }`). When no filter is given, prompts for confirmation before deleting the entire graph.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name whose graph is edited"
            },
            {
                "name": "filter",
                "type": "string",
                "required": true,
                "description": "SPARQL filter expression selecting the triples to delete; empty deletes all (after confirmation)"
            }
        ],
        "returns": {
            "type": "err|*",
            "description": "Throws an error or returns the callback result."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "access": "write"
    },
    {
        "name": "copyGraph",
        "module": "Sparql_generic",
        "description": "Copies all triples from a source's graph into another graph using paginated `with <fromGraph> insert { graph <toGraph> { ?s ?p ?o } } where { ?s ?p ?o }` runs, looping with `async.whilst` over `offset`/`limit` windows of 100000 until exhausted.",
        "params": [
            {
                "name": "fromSourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name whose graph is the copy origin"
            },
            {
                "name": "toGraphUri",
                "type": "string",
                "required": true,
                "description": "Destination graph URI"
            }
        ],
        "returns": {
            "type": "err|number",
            "description": "Throws an error or returns the last batch size."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "access": "write"
    },
    {
        "name": "getDistinctPredicates",
        "module": "Sparql_generic",
        "description": "Lists the distinct predicates actually used in a source, with their labels: the answer to \"what can I even ask about this source\". The natural precursor to a triple query, since it gives the property URIs to filter on rather than guessing them. Runs `select distinct ?p ?pLabel WHERE { ?s ?p ?o. optional { ?p ?x ?pLabel. filter(?x in (skos:prefLabel, rdfs:label)) } }` then fills missing labels via {@link module:Sparql_generic.setBindingsOptionalProperties}.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name to query"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Reserved options object"
            }
        ],
        "returns": {
            "type": "err|Array",
            "description": "Throws an error or returns SPARQL results with variables: `p`, `pLabel` (optional)."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "summary": "Lists the distinct predicates actually used in a source, with their labels: the answer to \"what can I even ask about this source\". The natural precursor to a triple query, since it gives the property URIs to filter on rather than guessing them.",
        "access": "read",
        "mcpTool": "sls_distinct_predicates"
    },
    {
        "name": "copyNodes",
        "module": "Sparql_generic",
        "description": "Copies selected nodes (and their properties) from a source into a target graph, optionally minting fresh URIs. Fetches each node's triples via {@link module:Sparql_generic.getNodeInfos}, remaps subject/object URIs (random hexa ids unless `keepOriginalUris`), can add `skos:exactMatch`/`rdfs:sameAs` back-links, parent links and `rdf:type` triples per the target schema, then INSERTs the rebuilt triples in batches of 100.",
        "params": [
            {
                "name": "fromSourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name the nodes come from"
            },
            {
                "name": "toGraphUri",
                "type": "string",
                "required": true,
                "description": "Destination graph URI"
            },
            {
                "name": "sourceIds",
                "type": "string[]",
                "required": true,
                "description": "URIs of the nodes to copy"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Copy options",
                "properties": [
                    {
                        "name": "keepOriginalUris",
                        "type": "boolean",
                        "required": false,
                        "description": "Reuse source URIs instead of minting new ones"
                    },
                    {
                        "name": "addExactMatchPredicate",
                        "type": "boolean",
                        "required": false,
                        "description": "Add an exactMatch/sameAs link back to the source node"
                    },
                    {
                        "name": "setParentNode",
                        "type": "Object",
                        "required": false,
                        "description": "`{sourceUri, targetUri}` to attach a parent (broader/subClassOf) link"
                    },
                    {
                        "name": "properties",
                        "type": "string[]",
                        "required": false,
                        "description": "Only copy these property URIs"
                    },
                    {
                        "name": "excludedProperties",
                        "type": "string[]",
                        "required": false,
                        "description": "Skip these property URIs"
                    },
                    {
                        "name": "additionalTriplesNt",
                        "type": "string[]",
                        "required": false,
                        "description": "Extra N-Triples strings to insert"
                    },
                    {
                        "name": "setSubjectFn",
                        "type": "Function",
                        "required": false,
                        "description": "Hook to mutate each item's subject"
                    },
                    {
                        "name": "setPredicateFn",
                        "type": "Function",
                        "required": false,
                        "description": "Hook to mutate each item's predicate"
                    },
                    {
                        "name": "setObjectFn",
                        "type": "Function",
                        "required": false,
                        "description": "Hook to mutate each item's object"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|number",
            "description": "Throws an error or returns the number of triples inserted."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "access": "write"
    },
    {
        "name": "sortBindings",
        "module": "Sparql_generic",
        "description": "Sorts SPARQL result bindings in place by the string value of a given field (missing values sort as empty string).",
        "params": [
            {
                "name": "bindings",
                "type": "Array<Object>",
                "required": true,
                "description": "Result bindings to sort"
            },
            {
                "name": "field",
                "type": "string",
                "required": true,
                "description": "Variable name to sort by (`binding[field].value`)"
            },
            {
                "name": "_options",
                "type": "Object",
                "required": false,
                "description": "Reserved (unused)"
            }
        ],
        "returns": {
            "type": "Array<Object>",
            "description": "The same array, sorted ascending by the field value"
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "setMissingLabels",
        "module": "Sparql_generic",
        "description": "Fills missing `<field>Label` cells in result bindings by deriving a label from the field's URI value (its local name) via {@link module:Sparql_common.getLabelFromURI}.",
        "params": [
            {
                "name": "bindings",
                "type": "Array<Object>",
                "required": true,
                "description": "Result bindings to enrich in place"
            },
            {
                "name": "_fields",
                "type": "(string|string[])",
                "required": true,
                "description": "Variable name(s) whose labels to backfill"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Reserved options object"
            }
        ],
        "returns": {
            "type": "Array<Object>",
            "description": "The same bindings with `<field>Label` cells added where missing"
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "setBindingsOptionalProperties",
        "module": "Sparql_generic",
        "description": "Backfills missing `<field>Type` and `<field>Label` cells for a family of indexed fields (`field`, `field1` … `field4`) in result bindings. Defaults the type to `skos:Concept` (or `options.type`) and derives labels from the URI local name; blank nodes get an empty label.",
        "params": [
            {
                "name": "bindings",
                "type": "Array<Object>",
                "required": true,
                "description": "Result bindings to enrich in place"
            },
            {
                "name": "_fields",
                "type": "(string|string[])",
                "required": true,
                "description": "Base variable name(s); indexed variants 0–4 are processed"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Enrichment options",
                "properties": [
                    {
                        "name": "noType",
                        "type": "boolean",
                        "required": false,
                        "description": "Do not add `<field>Type` cells"
                    },
                    {
                        "name": "type",
                        "type": "string",
                        "required": false,
                        "description": "Type URI to use instead of the default `skos:Concept`"
                    }
                ]
            }
        ],
        "returns": {
            "type": "Array<Object>",
            "description": "The same bindings with type/label cells filled in"
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "getLangFilterStr",
        "module": "Sparql_generic",
        "description": "Builds a SPARQL language filter that keeps a variable's literals in the requested language or with no language tag (`FILTER (lang(var)='xx' || !lang(var))`).",
        "params": [
            {
                "name": "options",
                "type": "Object",
                "required": true,
                "description": "Filter options",
                "properties": [
                    {
                        "name": "lang",
                        "type": "string",
                        "required": false,
                        "description": "BCP-47 language code to keep (alongside untagged literals); no filter emitted when absent"
                    }
                ]
            },
            {
                "name": "variable",
                "type": "string",
                "required": true,
                "description": "SPARQL variable expression (including `?`) the filter applies to"
            }
        ],
        "returns": {
            "type": "string",
            "description": "The language `FILTER` clause, or `\"\"` when no `options.lang` is given"
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    },
    {
        "name": "getSourceTaxonomy",
        "module": "Sparql_generic",
        "description": "Builds the complete subsumption taxonomy (class/concept hierarchy) of a source as a flat map of nodes with resolved ancestor chains. The broadest overview of how a source is structured, and large on a big ontology. The parent predicate and concept type are chosen from the schema (`rdfs:subClassOf`/`owl:Class` for OWL, `skos:broader`/`skos:Concept` for SKOS, `rdf:type`/`owl:NamedIndividual` for KNOWLEDGE_GRAPH, or `options.parentType`). The hierarchy query is paginated (limit 500) with `async.whilst`; results are then folded into `allClassesMap` (collecting labels/altLabels per node), each node's direct parents are expanded into a single most-specific ancestor chain, and orphan parents are back-filled (labels fetched via {@link module:Sparql_OWL.getLabelsMap} for OWL).",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name whose taxonomy is built"
            },
            {
                "name": "options",
                "type": "Object",
                "required": false,
                "description": "Build options",
                "properties": [
                    {
                        "name": "ids",
                        "type": "string[]",
                        "required": false,
                        "description": "Restrict to these subject URIs (adds a `setFilter` clause)"
                    },
                    {
                        "name": "filter",
                        "type": "string",
                        "required": false,
                        "description": "Extra SPARQL filter appended to the hierarchy query"
                    },
                    {
                        "name": "parentType",
                        "type": "string",
                        "required": false,
                        "description": "Parent predicate to use when the schema type is unrecognised"
                    },
                    {
                        "name": "withoutImports",
                        "type": "boolean",
                        "required": false,
                        "description": "Exclude imported graphs from the `FROM` clause"
                    }
                ]
            }
        ],
        "returns": {
            "type": "err|Object",
            "description": "Throws an error or returns `{classesMap, labels}`; each `classesMap` entry has `id`, `label`, `lang`, `skoslabels`, `parents`, `type`."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "summary": "Builds the complete subsumption taxonomy (class/concept hierarchy) of a source as a flat map of nodes with resolved ancestor chains. The broadest overview of how a source is structured, and large on a big ontology.",
        "access": "read",
        "mcpTool": "sls_source_taxonomy"
    },
    {
        "name": "createDecapitalizedLabelTriples",
        "module": "Sparql_generic",
        "description": "For every item whose preferred label is capitalised, inserts a `skos:altLabel` triple holding the decapitalised variant (improves search matching). Items are fetched via {@link module:Sparql_generic.getItems}, and the new triples are inserted in batches of 100.",
        "params": [
            {
                "name": "sourceLabel",
                "type": "string",
                "required": true,
                "description": "Source name to process"
            }
        ],
        "returns": {
            "type": "err|number",
            "description": "Throws an error or returns the number of triples inserted."
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": true,
        "example": null,
        "access": "write"
    },
    {
        "name": "triplesToTurtle",
        "module": "Sparql_generic",
        "description": "Stub intended to serialise an array of triples into Turtle (not yet implemented).",
        "params": [
            {
                "name": "triples",
                "type": "Object[]",
                "required": true,
                "description": "Triple descriptors to serialise"
            }
        ],
        "returns": {
            "type": "void",
            "description": ""
        },
        "responseSchema": "#/definitions/SparqlQueryResponse",
        "expose": false,
        "example": null
    }
]
